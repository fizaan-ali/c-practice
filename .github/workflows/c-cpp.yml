name: C Build & Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install build tools
      run: sudo apt update && sudo apt install build-essential -y

    - name: Compile C code
      run: gcc evenodd.c -o output

    - name: Run compiled output
      run: echo "Sample input" | ./output
